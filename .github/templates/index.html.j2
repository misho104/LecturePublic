<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ config.site.title }}</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>{{ config.site.title }}</h1>
    <p>{{ config.site.welcome_message }}</p>
    
    {% for directory, files in sorted_categories %}
    {% set metadata = category_metadata.get(directory, {'icon': '📁', 'name': directory, 'description': directory + ' materials'}) %}
    <h3>{{ metadata.icon }} {{ metadata.name }}</h3>
    <p class="category-description">{{ metadata.description }}</p>
    <ul class="pdf-list">
        {% for file in files %}
        <li class="pdf-item">
            <a href="{{ file.filename }}" class="pdf-name" target="_blank">{{ file.filename }}</a>
            <div class="pdf-info">
                {% if file.last_commit_date %}
                <span class="pdf-date">Updated: {{ file.last_commit_date }}</span>
                {% endif %}
                {% if config.display.show_version_badges %}
                    {% if file.is_old_version %}
                    <span class="old-version-badge">{{ config.display.old_version_badge_text }}</span>
                    {% else %}
                    <span class="version-badge">{{ config.display.version_badge_text }}</span>
                    {% endif %}
                {% endif %}
                {% if config.display.show_file_sizes %}
                <span class="pdf-size">{{ file.size }}</span>
                {% endif %}
                <a href="{{ file.github_history_url }}" class="pdf-history-link" target="_blank">History</a>
            </div>
        </li>
        {% endfor %}
    </ul>
    {% endfor %}
    
    <div class="footer">
        <p>{{ config.site.footer_text }}</p>
        <p>{{ config.site.license_text|safe }}</p>
    </div>
</body>
</html>
